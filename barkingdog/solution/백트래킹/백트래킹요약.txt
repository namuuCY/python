1. func의 i가 맨 마지막(n)에 어떤 행동을 할지 정한다.
    가령, 해당하는 모든 개수를 구하는거라면 count를,
    예시를 보이는 거라면 print는
1-(1) 또는 count나 print할 집합을 함수의 변수로 지정하는것도 가능

2-(1) 위의 과정과 관계없이 for구문과 isused를 사용해서
      모든 과정을 사용하고 (is used)표시
      재귀함수(k+1) 출력하고
      재귀가 끝나면 is used 표시를 원상태로 해놓기

2-(2) 두번째 선택지
C:\coding\python\barkingdog\solution\백트래킹\부분수열의합답지.py
이거처럼 함수 자체를 깔끔하게 설정하면 간단하게 표시도 가능.


3. 조합문제, 순열문제같은게 많은거같다.