트리는 그래프의 특별한 구조

    정확히는 무방향이면서 사이클이 없는 연결그래프
    (undirected acyclic connected graph)

## V개의 정점을 가지면 V-1개의 간선을 가짐
## 임의의 두 점을 연결하는 simple path가 유일하다.
## 임의의 노드를 루트로 만들 수 있다.

루트로부터 거리를 depth
자식이 없는 node를 leaf


트리에서의 BFS는 자식만 방문하고 확인하면 됨
vis배열 필요없이, 부모가 누구인지만 저장하고 있으면 된다.

